import styled from 'styled-components';

import { spacing, bgBlue2, rounded } from 'Utilities';

export const Card = styled.div`
  padding: ${spacing.md};
  background: ${bgBlue2};
  border-radius: ${rounded};
`;
