import React from 'react';
import { render } from '@testing-library/react';

import RecentProjects from './RecentProjects';

describe('<RecentProjects /> tests', () => {
  it('is being tested', () => {
    expect(true).toBeTruthy();
  });
});
